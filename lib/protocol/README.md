# Protocol Implementation Domain (Canonical)

`lib/protocol/` is the code implementation layer for protocol exchange.

Canonical subdomains:

- `control/`
- `rpc/`
- `binary/`
- `transport/`
- `internal/`

Non-code schemas live under `specs/`.
