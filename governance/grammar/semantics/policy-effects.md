# Policy Effects Semantics

Canonical effects are: `allow`, `deny`, `quarantine`, `review_required`, `degrade`, `require_justification`.
`deny` blocks action; `quarantine` allows constrained containment handling.
`review_required` enforces human/authorized escalation before final effect.
Normative effect declaration is distinct from runtime implementation behavior.
