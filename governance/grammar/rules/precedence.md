# Precedence Rules

Precedence must be explicit and deterministic.
Default conflict posture is fail-closed when unresolved.
`deny` and `quarantine` take priority over permissive outcomes unless explicit authorized override exists.
Rule priority values and stack order must be traceable.
