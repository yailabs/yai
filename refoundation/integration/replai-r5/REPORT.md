# R5 — obsolete YAI editor removal

Baseline master: `fec2f17b0e71e6e12e8f40a37c5f44d874f4b7d5`, tree
`0e3f5ba9062fe9fbfa8b0dfb18fa09b73ef649e5`. The published R4 anchor
`80d9077f24990711f9879b71c317fc2711d2d7fa` is followed by I05 target arbitration
and I06 cognitive execution intent. Those application changes are preserved.
Intended commit: `chore: remove obsolete vendored terminal editor`.
Pre-publication state: YAI removal and isolated product proof qualified;
all executable publication controls passed. An unrelated concurrent
constitutional document/guard failure is explicitly retained; exporting the
exact staged source independently makes that same guard pass. Awaiting scoped
commit/push and remote verification. This is the YAI part
of R5; cross-consumer completion belongs to the REPLAI roadmap.

## Removal and protected ownership

Removed `vendor/README.md` and `vendor/linenoise/{README.md,LICENSE,linenoise.c,linenoise.h}`.
The empty vendor roots are gone. Required-root and source-placement guards no
longer require or admit that obsolete source root. Current terminal docs and
third-party notices describe the actual dependency, retaining historical license
information in Git. The product PTY gate rejects vendor reintroduction.

REPLAI remains the exact native Cargo dependency at
`df5538c718b8d068432032e7fb116fb8bfab158e` (tree
`df79f4e025d212012edd9cdb4d0170a3ed5d7ebe`). No pin, Rust application/controller,
engine, C product, provider, cognitive or canonical-state implementation changes.
The only test enhancement allows the same product suite to run an explicitly
selected independently built YAI artifact instead of the default launcher.

Historical `subject:linenoise-terminal` identifiers in policy/authority fixtures
and compatibility records remain identifiers of non-authoritative application
subjects, not editor functions, selectable backends or vendored source. Renaming
those would alter unrelated persisted/application vocabulary. No duplicate editor
algorithm remains in them. R4 removed the active build script and FFI already.

Archaeology: the vendor arrived in current history at `f86c8a3` and lost its live
owner at R4. Rechecked `yai-dev` identity
`5c1c7b9d099eea9f2947146cd821d6501c4a6ddf` and the R4 recovered invariant:
terminal/projection input does not own application authorization or canonical
commit. The implementation property now lives in REPLAI plus the existing
conversation controller; retaining the old C editor recovers no missing behavior.

## Evidence and external scope

[Executable evidence](EXECUTION-EVIDENCE.md) retains exact commands, independent
source/build identity and real-product PTY/Case observations. The current I06
suite proves cognitive arbitration/intent/retry behavior as well as the original
editing-to-canonical submission boundary. There is no model acquisition.

YVEX EXTERNAL FINDINGS: `NO_ISSUE` — this YAI cleanup changes no provider contract;
no live external-provider request is claimed. Loopback HTTP exercises the actual
current YAI provider/controller path. Cross-consumer YVEX terminal qualification
is separate R5 work, not evidence imported from a YAI fixture.
