# Temporal-causal experience, Recall and persistent deliberation

Authority: bounded documentation/target-doctrine reconciliation evidence, not a
new implementation boundary or a second live status registry.
Baseline: `844a708aecda4a41ede552a2f7b329ac4c0338ed`; existing physical `master`,
HEAD/origin/master/remote master reconciled equal before editing.
Intended commit: `docs: clarify temporal experience recall and deliberation`.
The containing SHA and final publication equality belong in the handoff.

## Scope and archaeology

Directly re-read Roadmap, the full semantic-state target, Constitution and
Architecture, W20 report and current SEMANTIC.STATE.REFOUNDATION report;
inspect current memory hierarchy/compiler source, roadmap guard/regression,
navigation and pending External Golden evidence. No runtime property is deleted
or redesigned and no historical recovery is claimed.

- `semantic_state.rs::SemanticState::compose` verifies CaseState replay and
  retains history/current materialization; it is not an as-of validity engine.
- `memory_hierarchy.rs::MemoryEpisode` carries generations, exact Transition and
  typed relationship references. W20 epistemic class is separate from lifecycle;
  `w20_k04_k05_only_unique_newer_grounded_state_supersedes` qualifies bounded
  mechanical supersession, not arbitrary causal truth or temporal reconstruction.
- Existing roadmap S08/S09/S10, M02/M03/M05, C02/C06/C13 and Q06 absorb timescales,
  learned access, source qualification and refresh pressure. Those mechanisms
  do not already prove a question-conditioned Recall Trace or latent deliberation.

Before: qualified S → W → future E, with independent computational/semantic
evolution. After: qualified H plus current S → relevant reconstructed Recall →
bounded W → future E; six feedback processes preserve the same admission
asymmetry. H is no new ledger, Recall is disposable reconstruction, S preserves
epistemic/owner distinctions. E is reusable computational state; L is unfinished
deliberation, never a YAI memory owner. Model loss can invalidate E/L without
destroying admitted H/S. No equations become source types.

The target document owns detailed temporal/recall/refresh/assignment doctrine;
Roadmap owns programs, maturity and falsifiers. Architecture gets only an explicit
nonclaim clarification. Constitution already protects history/current/derived
and semantic/computational distinctions and is unchanged. No new canonical
document, parallel plan or memory program is created; this is a bounded report
under the existing evidence convention.

## Maturity and project-control delta

All 68 pre-existing maturity verdicts are preserved. Three new **OPEN** rows
have independent promotion/falsification boundaries:

| Row | Why existing rows are insufficient |
|---|---|
| S12 historical/as-of reconstruction | K01 sequence replay and S10 mechanical supersession do not answer event/observation/admission/validity-time queries under current disclosure |
| M06 temporal-causal Recall Trace | M03 traversal/M02 retrieval can find sources without qualifying a discontinuous trajectory, temporal meaning and exact source closure |
| E06 persistent deliberation | E04 finite composition and W01 progression do not qualify internal computation, persistent assignment budgets/deadlines/cancel or L resume/amnesia |

Counts are generated solely from the matrix: **32 ESTABLISHED, 23 PARTIAL,
12 OPEN, 4 LATER; 71 rows**. No existing green scope is enlarged. S08/S10,
C02/C06, M03/M05 and Q06 descriptions are extended without verdict promotion.
E05/Q03 evidence is reconciled to the current external checkpoint, not upgraded.
Programs K/S/M/C/A/E/W/Q clarify their roles; R/O/X/F and the selected primary
R/K/S/C/M/Q versus protected A/E/O/W/X distinction remain intact.

Spectrum reuses task-switch, replacement and amnesia controls, adding independent
discontinuous Recall/as-of, prompt-independent refresh and equal-compute persistent
deliberation falsifiers. Q06 includes temporal/causal/closure correctness,
assimilation/refresh latency, wrong-memory interference, State Read ablation and
task benefit, not fabricated benchmark thresholds. Learned navigation and SAE
remain optional research; Z_semantic is not model-native E. No training owner.

## External closure checkpoint

This is a **frozen read-only reconciliation of pending evidence and the operator
handoff on 2026-09-10**, not a new provider run or an external completion report.
The full pending package `refoundation/validation/external-golden-closure/` is
preserved byte-for-byte and remains outside this documentation commit, along
with its test-retention change. This checkpoint lets the published Roadmap cite
current status without depending on an untracked Markdown link or publishing
that unfinished repair. It does not replace its exact forensic package.

Pending report SHA-256 at reconciliation:
`5b414904a4453cea6d480fec2950a76d397499397d3d199540774212876de40e`.
The [validation capture](validation.jsonl), order 1, retains source-file digests,
original run identities/outcomes and unedited public-response excerpts read
from that package; order 4 preserves the attributed operator excerpt. The read
operation is a no-provider documentation check;
embedded older external observations retain their own run/order/pre-state.

| Evidence identity | Preserved observation |
|---|---|
| `external-golden-v3-generation2-20260910`, orders 1–3 | Public compat.v3, generation 2, 16,384 input/sequence capacity. Original exact body preflight 12,146 input / 4,238 output allowance; fresh-run postmortem 12,055 / 4,329. Requested output 0 means omitted max_tokens, not zero capacity. Neither preflight qualifies execution/resources. |
| `external-golden-generation2-product-20260910`, order 1, nested `yai-golden-free-xmf3p8vd` | Actual external Golden exit 2, 441.291 s. First `/work` failed the 300-second action wait after successful connection probes; no received response or ProviderResult; Workflow NOT_REACHED. No fixture fallback, cross-target route or generation retry. |
| `external-golden-generation2-regression-20260910`, order 1 | Existing deterministic publication + Golden local free/Workflow exit 0, 351.968 s; not rerun by this documentation task |
| `external-golden-generation2-compiler-20260910`, order 1 | Existing 8 compiler/locality/disclosure/delta/replay contracts exit 0; 8 versus 1,206 Transitions select 6 entries each; not new temporal Recall evidence |

Exact endpoint `http://127.0.0.1:18001`, model
`deepseek-v4-flash-mixed-mxfp4-release-v1`. Protocol 21, CUDA target-only and
YVEX revision `524efe7f67227f47a2fc749ca740f90d2294b937` are operator attestations,
not fabricated GET fields. No producer internals or runtime administration.
Fresh body: 40,277 bytes, SHA-256
`61a164eb3ae6f0c2683de6d0fdaa5dff5f1eb12bdef049c946c6c3d8d3fb248f`.
W: `working-state:sha256:ea8d46f1b39c9fd41712bfcf8a3569e1be24649c24911112599a9e6ba2a0c373`.
The earlier v2 413 and 512-token deployment are historical, not current blockers.

Francesco's correlated producer log report identifies PID 3709423, request
`oa-000..0014/r41`, session `oa-000000000014`. It reports 918/12,055 prefill tokens
after 293.21 s (3.13 tok/s average), zero generated, then cancellation/session
cleanup at approximately 300 s. Source command/file digest/exit were not supplied;
none is invented. The internally logged 499 was not received by YAI. Operational
logs do not create a canonical terminal result for the unresolved Invocation.

**External Golden FAIL; same-boundary closure BLOCKED.** Prefill latency is
localized, its internal bottleneck unknown: YVEX_CANDIDATE for producer diagnosis.
The YAI harness/transport observation-deadline gap is independent. No arbitrary
timeout/context increase, semantic truncation or blind retry follows. This docs
commit is not an external-closure completion and selects no next implementation.

## Validation and preservation

Use existing roadmap summary generation, documentation/layout/links, roadmap
regression, topology regression/static audit and whitespace checks. Retain actual
commands/cwd/environment/pre-state/run/order/exits in [validation.jsonl](validation.jsonl).
Audit the complete staged docs whitelist, link targets in the staged tree,
unchanged old maturity verdicts and preserved pending evidence before commit.
Validation state is recorded below after execution, not inferred from Markdown.

Run `temporal-causal-docs-20260910`, repository-root cwd, `TMPDIR=/tmp`,
`no_provider` documentation/validation-infrastructure proof:

| Order | Actual command / audit | Result |
|---|---|---|
| 1 | Read existing public-response evidence/run metadata and file hashes | exit 0; no provider request, separate original run identities retained |
| 2 | `make check-docs check-layout test-roadmap test-topology` | exit 0; local links/layout/counts and 8 roadmap / 10 topology assertions pass |
| 3 | Recorded inline baseline/live-snapshot/maturity/protected-file audit | exit 0; all 68 verdicts and 32 green rows unchanged, live operational selection preserved, no runtime/guard delta |
| 4 | `check-roadmap.py --summary`, static topology audit, `git diff --check`, operator-excerpt read | exit 0; 71 maturity rows; 483 classification entries, 71 historical/86 current Make leaves and zero combined duplicates |
| 5 | Recorded inline staged whitelist/link/pending-evidence audit | exit 0; exactly five documentation/evidence files, 110 local references/anchors resolve in INDEX, all 38 pending external files byte-identical |
| 6 | Final `make check-docs check-layout test-roadmap test-topology` | exit 0; all documentation/layout/count and roadmap/topology regression gates PASS |

Documentation qualification and complete staged-diff review **PASS**. Final
report/journal metadata is restaged and whitespace/whitelist checks precede
commit/push and remote equality verification. No external completion is implied
by this pre-publication documentation result.

Runtime/Transition/CaseState/other schema delta **0**; semantic/operational
owners **+0/+0**; LMDB **+0**, still **37/40**. Transition v18 / CaseState v15,
Projection/ContextFrame v9, RenderedInput v7 and S/W/delta v1 are unchanged.
No source, dependency, guard implementation, producer contract or test semantics
change. Root README, pending provider documentation and test-retention edits
are outside the whitelist. The pre-existing dirty worktree is intentionally
preserved, not described as clean.

AUTOMATED REGRESSION and GOLDEN LOCAL: **NOT_AFFECTED** by this docs-only update;
previous actual passes above retain their scope. No new runtime/external test.
YVEX EXTERNAL FINDINGS: no new observation; current failure retained, not repaired.
CONTINUITY CANARY **NOT_RUN**; HUMAN_GOLDEN_CASE **PENDING_OPERATOR**;
ZERO-TO-CURRENT **unchanged** because no command, prerequisite or lifecycle changed.
I01–I06 COMPLETE; **I07 UNSELECTED**. No E consumer, State Read/Update, H20/W21/W22,
REPLAI/YVEX change, adapter/training or implementation boundary starts here.
