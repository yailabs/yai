# YAI README and public identity

Authority: bounded documentation/asset delivery evidence, not live project control.
Baseline: `6e898e2830fe3ee05a1d891477a2967fa9274ef2`, reconciled with
`origin/master` and remote master in the existing clean physical worktree.
Intended commit: `docs: refresh public README and YAI branding`.

The owner explicitly requested the root README rewrite, the existing YAI brand
kit, a copy to Spark, the organization avatar and the repository About.
YVEX's public README at `3f4a1c182d35e5a0e163adb81008ae7a366efcc6`
provided the presentation structure only; no YVEX source/runtime was changed.

## Delivered scope

- Root README: centered theme-aware identity, current product introduction,
  bounded implemented properties, short guided setup, Case actions, ownership,
  qualification, navigation and explicit current limits. The previous text
  incorrectly still described pre-refoundation authority and direct-write gaps.
  Current architecture, Golden/guided evidence and the actual controller/CLI
  were inspected before replacing those claims.
- `docs/assets/brand/`: three unchanged owner-supplied PNG exports, provenance,
  usage and SHA-256 manifest. The full 39 MiB export kit is not added to Git.
- Full kit copied from the operator's `Pictures/YAI-brand-kit` to
  `/home/dgmothx/Pictures/YAI-brand-kit` on `spark-7c3d`. Transfer did not
  overwrite existing files; destination checksum verification succeeded.
- Repository About changed from `the harness  u need.` to:
  “Governed work and durable semantic continuity: Case state, policy, resources
  and cognitive execution, independent of any model or provider.”
  The existing homepage is unchanged. GitHub metadata is not part of a Git commit.
- Organization avatar **not uploaded**: the authenticated account is an
  organization admin, but the available GitHub integration has no avatar upload
  action and no authenticated browser-control surface is available. The avatar
  file is ready; no undocumented upload endpoint or browser credential extraction
  was attempted. Asset presence is not reported as a changed organization avatar.

## Validation and limits

[validation.jsonl](validation.jsonl) retains ordered real commands, baseline,
pre-state, cwd, relevant environment, exits and bounded raw output. Gates cover
documentation/layout, roadmap/topology assertions, classification reachability,
image references/hashes and whitespace. GitHub's Markdown renderer preserves
the centered picture and theme sources. The three images were visually inspected;
no claim of a human Golden run or runtime qualification follows.

No runtime source, schema, semantic/operational owner, LMDB database, provider
behavior or qualified REPLAI pin changes. I07 and semantic-state implementation
remain untouched. Runtime and Golden gates are not rerun for this docs/assets
scope; existing model/deployment limitations remain explicit.

Pre-publication: documentation/assets validated; only this task's whitelist is
eligible for staging. Final containing SHA, push and remote equality are verified
after commit, not self-recorded here. The remaining organization-avatar upload
is a separate browser action, not a reason to claim this entire request complete.
