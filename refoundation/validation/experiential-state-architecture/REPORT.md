# EXPERIENTIAL.STATE.ARCHITECTURE.1

Authority: bounded documentation/project-control alignment evidence.
Baseline: `a0af833323ddb83b62f77f98c6094110723e9690`; actual HEAD,
origin/master and remote master reconciled equal in the existing clean physical
master worktree. Intended commit:
`docs: align semantic working and experiential computational state`.
Final containing SHA and publication equality belong in the post-commit handoff.

## Reconciliation and scope

The specified `09a5c08` is historical. Published `6e898e2` had already introduced
W_t, independent computational updates, the qualified KV bridge and primary
R/K/S/C/M/Q scope. This pass builds on that correction, preserving subsequent
README/branding, repository cleanup, current REPLAI pin and all product evidence.
No historical report is rewritten and no runtime mechanism is redesigned.

Re-read the complete current Roadmap, linked target doctrine, roadmap guard and
regression, prior working-state correction and constitutional continuity/authority
context. Current Architecture still labels the general compiler and state
contracts unimplemented and needs no mutation. Root README is protected and
unchanged. The existing guard needs no new literal-prose rule or count registry.

BOUNDARY is not invoked: this changes no producer API/ABI, public executable
contract, dependency, consumer manifest or integration obligation. It records a
future conceptual boundary, not a resolver result or cross-repository delivery.
No producer internals, YVEX administration or REPLAI migration are involved.

## Architectural delta, not implementation evidence

- S_t is durable model-independent Case meaning under existing admission owners.
  W_t is YAI-compiled bounded task-relative state under intent, Participant
  authority/disclosure, relevance and budget. E_t is YVEX's derived, replaceable,
  model-native Experiential Computational State. Symbols are not source types.
- `W_t = Compile(S_t, I_t, A_t, R_t, B_t)`;
  `E_t = Lower(W_t, Model, StateProfile)`;
  `Exec(X_t, E_t) → (Y_t, E_t+1, P_t optional)`.
  E_t may evolve without changing semantic Case truth. Explicit proposals or
  observed consequences need typed normalization and applicable admission;
  existing Invocation/Result/effect evidence is not removed.
- “Experiential” means execution-accumulated computational state, not
  consciousness, emotion, identity or personhood; it is not a semantic ledger.
- The ladder distinguishes current context footholds, unqualified persistent
  context-derived state, experiential read, external-delta update, learned
  read/write and native dual-stream. A scoped qualified KV/prefix bridge remains
  possible without training; persistence alone proves no State Read semantics.
- State-augmented pretrained backbones, exact model-side adapters/profiles and
  targeted post-training are research options, including an initially frozen
  backbone. No adapter, training pipeline or dataset/database owner is created.
  Cases/Golden may supply scoped semantic evaluation trajectories, not automatic
  truth labels or permission to export private data.
- Semantic immediate/task/Case/slow timescales remain OPEN, not tensor geometry.
  Spectrum now distinguishes whole Case versus W_t, full reconstruction/delta,
  training-free versus learned read/write, E_t amnesia and model recompilation.
  These are falsifiers, not a queue or current benchmark results.

All 68 maturity identities/verdicts remain unchanged: 28 ESTABLISHED,
22 PARTIAL, 14 OPEN, 4 LATER. No green evidence row is changed. State Read/Update,
the general W_t compiler and multi-timescale contracts remain OPEN. Explicit
nonclaims cover E_t inside YAI, native experiential-stream certification and
state-augmented training. S08, C06, C11/C12/C14 and Q06 scope text is sharpened.

SEMANTIC.STATE.REFOUNDATION.0 remains SELECTED_NOT_STARTED, primarily
R/K/S/C/M/Q; A/E/O/W/X remain protected invariants/consumers. Its future after-state
is explicit semantic meaning, W_t compilation, semantic full/delta equivalence
and context compatibility with a clean future public boundary, never E_t in YAI.
I01–I06 remain complete and I07 unselected.

## Qualification and pre-publication state

[validation.jsonl](validation.jsonl) retains actual ordered commands, cwd,
environment, pre-state, baseline, exits and bounded unedited stdout/stderr.
Use documentation/layout/roadmap guards, existing unit/regression roadmap and
topology assertions (no_provider), static classification reachability, generated
counts and a baseline comparison of every maturity verdict/green evidence row.
The focused audit also checks unchanged selected ownership, formulas, protected
qualification postures and the exact docs-only file whitelist. These checks
validate documentation consistency, not compiler or experiential-state behavior.

Runtime/schema delta 0; Transition v18 / CaseState v15 unchanged; semantic and
operational owners +0; LMDB +0, still 37/40. No runtime source, test semantics,
guard implementation, provider integration or dependency revision changes.
Full runtime/publication and Golden execution are NOT_AFFECTED by this docs-only
scope, not newly claimed PASS. Existing local Golden evidence is preserved.
External YVEX and canary NOT_RUN; HUMAN_GOLDEN_CASE = PENDING_OPERATOR.
ZERO-TO-CURRENT unchanged: no product commands or lifecycle behavior change.
YVEX EXTERNAL FINDINGS: no new findings; recorded probe costs and Case HTTP 413
remain unresolved. No live provider or training experiment was executed.

Run `experiential-state-architecture-20260908`, repository-root cwd, TMPDIR=/tmp:

| Order | Actual command / proof | Outcome |
|---|---|---|
| 1 | `make check-docs check-layout test-roadmap test-topology` | exit 0; documentation/layout/count guards, 8 roadmap and 10 topology assertions PASS, no_provider |
| 2 | Inline baseline/notation/scope audit retained verbatim in command evidence | exit 0; all 68 verdicts and green evidence rows unchanged, exact primary selection and protected qualification snapshot preserved, two-doc/evidence whitelist PASS |
| 3 | `python3 tools/validation/topology.py audit --static` | exit 0; 71 historical smoke leaves preserved, 86 current Make leaves, zero combined duplicates; classification PASS |
| 4 | `git diff --check` | exit 0, empty output |

Pre-publication documentation qualification PASS. The complete isolated staged
diff is reviewed before commit/push; final SHA and remote equality are verified
separately. This document authorizes no source refactor or new wave.
