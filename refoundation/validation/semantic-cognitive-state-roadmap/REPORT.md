# SEMANTIC.COGNITIVE.STATE.ROADMAP.0

Authority: bounded documentation/project-control evidence, not a second roadmap.

Baseline: `729fbbf9eea7c89e81f77965f52d9d98b01bffed`, clean physical master,
HEAD/origin/master/remote master reconciled before editing. This is newer than
the specification's `953f3d8`; Golden, guided CLI, connection discovery/qualification,
the operator's qualified REPLAI repin and conversation presentation are preserved.
Intended commit: `docs: refound roadmap around semantic cognitive state`.
The containing commit SHA belongs to the final publication handoff, not this file.
Documentation/guard implementation and qualification are complete. Pre-publication
state: ready for isolated staged-diff review and commit/push; remote master was
rechecked at the preserved baseline, with no concurrent divergence observed.

## Scope and authority decision

Replace the historical-wave-centered ROADMAP with one living control surface:
snapshot, sole generic maturity matrix, programs, adversarial spectrum, one
selected execution boundary, dependency horizons, qualification, traceability,
nonclaims and promotion/update rules. Git owns chronology; all old reports remain.
No I07, post-I10 runtime work, H20/W21/W22, Studio or source refactor begins.

Adopted thesis: YAI owns semantic cognitive state; YVEX owns computational
cognitive state. State Read and State Update are OPEN target capabilities now.
Context/Projection are compatibility footholds, not the final definition of
memory. Provisional SemanticStateFrame/Delta are not schemas, source types or
owners. `M_t = Lower(S_t, exact model, exact state profile)` preserves durable
semantic meaning without promising identical hidden activations. Computational
updates cannot be the sole copy of admitted Case truth.

The selected next boundary is **SEMANTIC.STATE.REFOUNDATION.0,
SELECTED_NOT_STARTED**. It needs a bounded implementation contract and explicit
source authorization. I01–I06 COMPLETE; I07 UNSELECTED. Former post-I10 naming
does not force invented Interlock milestones or supply runtime authorization.

The Constitution already establishes continuity, derived-state hierarchy,
admission, payload ownership and Agent non-ownership: no constitutional edit is
needed. ROADMAP owns maturity/selection; Architecture owns source truth; the
target document elaborates adopted doctrine/hypotheses without a second queue.
ZERO-TO-CURRENT remains the sole cumulative human runbook and is unchanged
because this closure adds no product action or semantic behavior.

## Direct archaeology and bounded maturity evidence

Read current ROADMAP, Constitution, Architecture, navigation, semantic-state
target, ZERO-TO-CURRENT, provider governance and reference context/boundaries /
governance. Re-read W19/H19/W20, I01–I06, TEST.TOPOLOGY.0, REPLAI R4/R5,
Golden, guided CLI and subsequent connection/presentation evidence. Inspect
actual validation classification, Make graph and documentation guards.

| Family | Executable inspection | Consequence for classification |
|---|---|---|
| Canonical state | `engine/yai-engine/src/transition.rs`, `store/lmdb.rs` reducers, commit/replay and current DB opens | Atomic history/materialization and known readers are bounded established properties; no generic SemanticState or objective lifecycle inferred from legacy Record nouns |
| Authority/security | `admission.rs`, typed policy/Decision/review contracts; W9–W12 and Golden evidence | READY/admission/generation/scope proven in current bounds, not enterprise identity or universal governance |
| Preparation | `context.rs`, current Projection/ContextFrame v8 and RenderedInput v7 | Bounded authority-aware compilation foundations, not a State Compiler or Case-age locality |
| Cognitive execution | `cognitive.rs`, current v2 binding/planner, I03–I06 exact realization/composition/host evidence | Exact pinned/arbitrated target semantics established; full cold-model substitution remains partial |
| Operational access | `admission.rs`, `effect/access.rs`, `resource_control.rs`, CLI resource transport; Golden report | Real confined filesystem/process/SQLite/HTTP/MCP verticals, not a universal connector or ambient tool API |
| Derived state | `memory_hierarchy.rs`, `memory_index.rs`, graph and compatibility producers; W19/H19/W20 | Episodes/assertions/indexes remain evidence-bound/rebuildable; generic graph/analytics maturity is partial |
| Workflow/Handoff/runtime | `workflow.rs`, `handoff.rs`, `runtime_instance.rs`, W17/H17, H13 and Golden | Bounded progression, exact Handoff and single-host scheduling; no distributed owner or federation claim |

Every green matrix row links bounded source/test/report evidence. Links are
traceability, not automatic proof of generic maturity. Current package version
strings do not establish an authorized v0.1 release scope; no named scope was
found in release/product documentation, and none is invented.

Legacy gate: read `yai-dev@5c1c7b9d099eea9f2947146cd821d6501c4a6ddf`,
`Documentation/architecture/05-quality-attributes/maturity-and-claim-boundaries.md`
and `Documentation/product/maturity.md`, with history `69e4aa6a3`, `c580ea05a`,
`aafebe306`. No root ROADMAP/history at that path supplies a missing owner.
Recover implementation honesty and the separation of evidenced/unfinished /
not-claimed capability. Target owner is the single live ROADMAP. Reject the
historical parallel maturity documents, obsolete Series-2 ownership/tree and
any readiness/compliance inference from documentation. No historical runtime
code or YVEX repository is copied or administered. The requested YVEX-style
control discipline is taken from the specification, not represented as a fresh
inspection of YVEX's own roadmap.

## Documentation reconciliation

- ROADMAP: replace wave chronology with 68 scoped maturity rows and 12 programs
  R/K/A/S/C/E/O/W/M/X/Q/F; retain historical evidence anchors.
- Target doctrine: preserve Case/Agent/payload/access/falsifier invariants;
  distinguish adopted dual-stream State Read/Update from hypothetical mechanisms;
  remove the obsolete prerequisite of inventing I07–I10.
- Navigation: point to the sole replace-in-place snapshot, not an R5-era duplicate.
- Architecture/provider/reference context/boundaries: remove contradictory
  current v5/v13/pre-I03 claims, old “no vector retrieval/TLS/scheduler” claims,
  and the pre-Golden single-resource boundary. Preserve historical reports and
  explicitly label current bounded implementation versus future state concepts.
- Testing/checks documentation: document minimal docs-only gates and read-only
  summary generation, without weakening implementation publication gates.

Root README, Constitution, ZERO-TO-CURRENT, REPLAI pin/documentation/evidence,
runtime source and all historical wave reports are unchanged.

## Validation contract

`tools/checks/check-roadmap.py` uses only the marked ROADMAP System Maturity
rows. It validates unique IDs, exact states, evidence links, local reference
targets/headings, generated summary and one selected temporal boundary matching
the snapshot. It rejects competing root/docs live control files/declarations,
not archived historical evidence. `--summary` only prints the recomputed line.
No second registry, persistent result cache, network or maturity promotion.

The existing constitutional/documentation guard now checks the actual
`Authority: living public project control.` invariant instead of requiring the
obsolete `implementation delta` label. `make check-docs` includes `check-roadmap`.
`test-roadmap` is classified **unit / regression / no_provider / fast** and
automatically participates in the existing fast/release graph. Negative tests
reject stale counts, missing rows/references, invalid states, duplicate IDs,
competing authorities and incoherent/duplicate selected boundaries. Other
tables cannot inflate counts. No existing test/classification is removed.

Actual command evidence is retained in [validation.jsonl](validation.jsonl)
using the existing `../provider-connect-hardening/capture.py`. Each record has
run ID, invocation order, material pre-state, cwd, exact command/environment,
baseline SHA, exit, elapsed time and bounded unedited stdout/stderr. Tests use
temporary metadata only; no operator YAI home or continuity canary is opened.
No product identifiers are produced by a documentation gate.

Recorded qualification, run `semantic-cognitive-state-roadmap-20260908`:

| Order | Command / property | Actual result |
|---|---|---|
| 1 | `make check-roadmap check-docs check-layout test-roadmap test-topology` | exit 0; 1.02s; docs/layout pass, 8 roadmap refusal/read-only/count assertions and 10 topology assertions pass; no provider |
| 2 | `python3 tools/validation/topology.py audit --static` | exit 0; 0.06s; all 71 old smoke leaves reachable, 86 current Make leaves, zero duplicate leaves in combined check/characterization; 474 classified entries / 90 assertion files / 372 Rust selectors, not 372 newly executed Rust tests |
| 3 | `git diff --exit-code HEAD -- README.md AGENTS.md docs/constitution.md docs/zero-to-current.md docs/replai-terminal.md engine cmd include system` | exit 0 with empty output; no runtime/schema/pin/constitutional/runbook/root-README mutation |
| 4 | `git diff --check` | exit 0; no whitespace errors |
| 5 | `make check-docs check-layout test-roadmap test-topology` | exit 0; 1.03s; final combined docs/links/layout/counts and 8 + 10 no-provider assertions pass |

The derived maturity summary is 28 ESTABLISHED, 22 PARTIAL, 14 OPEN, 4 LATER,
68 total rows; no percent-complete claim. Full runtime regression and Golden
are not rerun: their published evidence is cited, not relabeled as new
execution. Final docs/diff checks and staged ownership inspection precede
publication; the post-commit handoff records remote equality.

## Ownership/version and acceptance posture

Runtime semantics 0; Transition/CaseState schema delta 0 (remain v18/v15);
ProviderQualification remains v5. Semantic owners +0, operational owners +0,
LMDB databases +0, current total 37/40. Documentation validation is the only
new executable tooling, not application behavior or an authority owner.

| Evidence surface | Posture for this closure |
|---|---|
| AUTOMATED REGRESSION | PASS documentation/unit/reachability, no_provider; actual commands/exits retained above |
| GOLDEN CASE LOCAL | NOT_AFFECTED: no runtime, resource, policy, CLI, schema or runbook change; prior published Golden PASS remains bounded historical evidence |
| GOLDEN CASE EXTERNAL YVEX | NOT_RUN here; complete external lifecycle remains unqualified |
| CONTINUITY CANARY | NOT_RUN; private operator-owned data untouched |
| HUMAN GOLDEN CASE | PENDING_OPERATOR; automated PTY/probes are not human acceptance |
| ZERO-TO-CURRENT | Unchanged with reason: no product behavior/commands change; mandatory future-wave cumulative role preserved |

## YVEX EXTERNAL FINDINGS and remaining pressure

No new provider integration/realization or runtime behavior changes; no live
request is executed in this documentation closure. Preserve the exact
[single-connect evidence](../single-connect/REPORT.md): public endpoint
`http://127.0.0.1:18001`, exact exposed model
`deepseek-v4-flash-mixed-mxfp4-release-v1`, real synthetic qualification succeeds;
operator's subsequent generation-14 Case SEND receives HTTP 413
`request_too_large` and remains delivery-indeterminate. The report distinguishes
automated run IDs from operator-reported Turn/binding IDs. No full external
Golden PASS or Qwen/cold-substitution proof follows.

Existing classification remains: **YAI_DEFECT** setup probe overlap/cost;
**YVEX_CANDIDATE** public-capacity pressure requiring independent confirmation,
not an inferred private implementation defect. This roadmap neither fixes those
issues nor interprets opaque runtime state. Future cognitive-state capabilities
are adopted OPEN contracts, not evidence of an existing public YVEX ABI.

Before semantic-state source refactoring: establish the minimum state
producer/consumer and admission/supersession contract; separate canonical and
derived material; specify scoped working-set budget, full/delta equivalence,
compatibility consumer and replay/amnesia/Golden negatives. Determine versions
only after serialized meaning is known. No preselected new owner/database,
model-state layout or private cross-project protocol is authorized.
