# kernel/lib/internal

Internal-only kernel lib headers.

Headers here are private to kernel implementation code and must not be exposed as
public subsystem surfaces unless they become stable canonical APIs.
