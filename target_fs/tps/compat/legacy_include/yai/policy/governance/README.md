# Governance Public Headers (Canonical)

`include/yai/policy/governance/` is the canonical public governance namespace of the
unified YAI system.

All public governance includes are canonical under `yai/policy/governance/*`.
