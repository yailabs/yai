# sup

`target_fs/sys/sup/` is the supervisor service surface.

It keeps only the canonical supervisor service entry shell under `cmd/`.
Supervisor runtime implementation belongs to `target_fs/krt/sup/`.
