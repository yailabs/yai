# Fallback Resolution Rules

When domain confidence is insufficient, runtime must apply explicit fallback strategy.
Fallback outcomes are policy-controlled (review/quarantine/deny) and traceable.
Fallback must never bypass evidence obligations.
