# Policy-native cognitive Case resources

A compact enterprise policy in the supported JSON, Markdown policy-block and
text-PDF profiles. These are external source fixtures, not a policy engine.
The six rules permit scoped reads, deny search and require eligible human
review with an audit reason for writes. All forms use the same existing IR.

Import each format in an independent fresh home/Case: same lineage/version with
changed original bytes deliberately collides. `ambiguous.md` must remain
unresolved; it cannot be published as policy. No real-model response is embedded.
