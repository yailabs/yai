[package]
name = "yai-studio"
version = "0.0.0"
edition = "2021"
publish = false

# Independent desktop package; never a member of the engine workspace.
[workspace]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
