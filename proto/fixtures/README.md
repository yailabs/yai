# NET compatibility fixtures

Authority: deterministic examples for the current versioned schemas. Fixture
presence is not runtime capability or architectural adoption.

- `net/` contains versioned NET fixture examples, including stream, node and
  capability fixtures.
