# Docs

Docs root has been collapsed for pre-O1 filesystem alignment.

- Canonical live entry for historical/editorial material: `docs/transitional/`
- Runtime/tooling contracts are now defined by code/layout roots (`kernel/`, `sys/`, `user/`, ``), not by old docs topology.

This collapse is intentional and temporary until post-cutover doc normalization.
