# Data Plane — Milestone Packs

This directory contains phase-closure milestone packs for `RB-DATA-PLANE`.

Phases:
- v5.0 Layout + manifest
- v5.1 Kernel authority store (LMDB)
- v5.2 Engine event store (DuckDB baseline)
- v5.3 Optional STM (Redis)
- v5.4 Unified data CLI (kernel-governed)

Each MP MUST include:
- scope + acceptance checklist
- exact commits/PR refs (filled at closure)
- evidence pointers to captured command outputs under `evidence/`