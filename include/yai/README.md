# C header source map

Authority: local navigation for the current C ABI. Canonical concept meanings
are owned by [`docs/reference/semantics.md`](../../docs/reference/semantics.md).

Headers here describe interfaces compiled by the narrow `yaid` product subset
or by characterized C components. Presence does not prove product reachability,
a source owner, an installed ABI, or target adoption. Names including Subject,
CaseWorld, broad RecordKind values, and model output as EffectReceipt remain
current compatibility vocabulary rather than refounded semantics.

The surface was reduced from 101 to 55 headers. No external consumer or header
installation step is known; daemon JSON, persisted record shapes, and the
surviving platform mechanics remain the specific compatibility risks. Do not
infer future directories or authority from this layout.
