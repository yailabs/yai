# yai/graph

Transitional YAI-native bucket.

This directory is NOT considered final canonical surface.
Contents must be progressively redistributed into:
- include/ canonical subsystem roots
- include/yai/ Linux-equivalent kernel surfaces

Keep only files that remain justified as stable YAI-native kernel APIs.
