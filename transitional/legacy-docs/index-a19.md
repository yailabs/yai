# A19 Legacy Docs Index

This index captures known documentation surfaces that still contain legacy
references after A17/A19 cutovers.

## High-priority legacy reference clusters

- test topology old names (`workspace_lifecycle`, `runtime_handshake`,
  `source_plane`, `law_resolution`, `core_brain`, `core_exec`)
- governance old runtime paths (`lib/law/*`, `include/yai/law/*`)
- embedded-law as primary runtime truth (`embedded/law` narrative in active docs)

## Representative locations to clean in Block B

- `docs/architecture/`
- `docs/developer/`
- `docs/program/reports/`
- `docs/program/23-runbooks/`
- `docs/program/audit-convergence/`

## Handling policy

- canonical docs should point to final domains only
- historical docs may remain but must be explicitly marked historical
- compatibility-only references must be labeled non-canonical
