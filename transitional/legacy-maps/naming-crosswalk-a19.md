# A19 Naming Crosswalk

Canonical naming direction:

- `law` runtime domain -> `governance`
- `brain` runtime domain -> `knowledge` and `orchestration` (as applicable)
- `source_plane` path style -> `source-plane`
- `yai_mind_*` symbol prefix -> `yai_*`

Notes:

- compatibility labels may still appear in historical docs and reports
- canonical new code and test surfaces must not introduce legacy naming
