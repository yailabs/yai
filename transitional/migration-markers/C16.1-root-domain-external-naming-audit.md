# C16.1 Root and Domain External Naming Audit

Date: 2026-03-11
Owner: yai
Status: applied

## Scope

External naming pass across repository root, top-level domains, root framing files,
and root-facing validation checks.

## Decisions Applied

### Renamed now

- `archive_tmp/` -> `archive/`
- `DATA_POLICY.md` -> `DATA.md`

### Canonical root language confirmed

- `cmd/`, `include/`, `lib/`, `governance/`, `foundation/`, `formal/`, `docs/`, `tests/`, `tools/`, `data/`, `transitional/`
- `runtime`, `edge`, `mesh`, `orchestration`, `protocol`, `providers`, `agents`, `knowledge`, `graph` retained as canonical domain names.

### Transitional / non-canonical explicit

- `archive/` remains non-canonical and migration-adjacent.
- `transitional/` remains migration-only and non-1.0.0 canonical.

## Guardrails Updated

- `tools/validate/validate_root_topology.py`
  - forbids `archive_tmp` at root
  - validates C15 formal external subtree (`audit`, `artifacts`, `configs`, `models`, `modules`, `schema`, `traceability`)
- `tools/validate/validate_unified_repo_root_framing.py`
  - adds `DATA.md` framing checks

## Deferred to next naming passes

- Broad internal symbol/header/implementation naming normalization (C16.2+).
- Historical references outside root-facing framing (C17/C18 cleanup path).
