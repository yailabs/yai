{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "yai.governance.agent_governance_action.v1",
  "title": "Agent Governance Action Contract v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "action_id",
    "action_type",
    "target_object_id",
    "proposal_mode",
    "requires_confirmation",
    "source_refs",
    "status",
    "runtime_family_owner",
    "runtime_binding_model",
    "topology_target"
  ],
  "properties": {
    "action_id": { "type": "string", "minLength": 3 },
    "action_type": {
      "type": "string",
      "enum": [
        "propose_parse",
        "propose_normalize",
        "propose_build_candidate",
        "propose_review_submit",
        "request_review_approval",
        "request_dry_run",
        "request_apply_attach",
        "request_apply_detach"
      ]
    },
    "target_object_id": { "type": "string", "minLength": 3 },
    "scope_target": { "type": ["string", "null"] },
    "proposal_mode": {
      "type": "string",
      "enum": ["assistive", "proposal_only", "request_transition"]
    },
    "requested_transition": { "type": ["string", "null"] },
    "explanation": { "type": "string" },
    "source_refs": {
      "type": "array",
      "items": { "type": "string" }
    },
    "confidence_hint": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0
    },
    "requires_confirmation": { "type": "boolean" },
    "allowed_by_policy": { "type": "boolean" },
    "status": {
      "type": "string",
      "enum": ["proposed", "accepted_for_review", "blocked", "executed"]
    },
    "runtime_family_owner": {
      "type": "string",
      "enum": ["core", "exec", "data", "graph", "knowledge"]
    },
    "runtime_binding_model": {
      "type": "string",
      "enum": ["scope-first"]
    },
    "scope_binding_required": { "type": "boolean" },
    "topology_target": {
      "type": "string",
      "const": "unified-runtime-v1"
    },
    "disallowed_subsystem_targets": {
      "type": "array",
      "items": { "type": "string", "enum": ["brain", "mind"] }
    }
  }
}
