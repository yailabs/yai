# Override Rules

Overrides are allowed only with explicit authority requirements.
All overrides must emit traceable justification and evidence obligations.
Time-bound overrides require expiry metadata.
