# Domains Semantics

A governance domain is a resolvable normative module, not a label.
Each domain module binds discovery -> model -> policy -> evidence obligations.
Domain families allow shared semantics with controlled specialization.
Descriptive domain packs in `ops` are not runtime authority.
Runtime authority reads domain logic from `governance/domains`.
