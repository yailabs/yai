# Overlays Semantics

Overlays are scoped extensions (`session`, `scope`, `organization`, `experimental`).
They can refine applicable policies but cannot arbitrarily rewrite foundation invariants.
Overlay precedence and authority requirements must be explicit and traceable.
